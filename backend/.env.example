SECRET_KEY=changeme
DEBUG=True

ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=centro_analisis
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001

# Poner en 1 solo cuando se prueba por túnel (frontend y backend en hosts distintos)
DEV_TUNNEL=0
